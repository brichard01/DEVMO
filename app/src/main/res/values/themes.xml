<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DEVMO" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Composant titre par défaut-->
    <style name="Titre">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">48sp</item>
        <!-- <item name="android:font"></item> -->
    </style>

    <!-- Composant bouton par défaut-->
    <style name="Bouton">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">24sp</item>
    </style>

    <!-- Composant bouton par défaut-->
    <style name="Autre">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">40sp</item>
    </style>
</resources>